version: "2"
services:
  mockbin:
    build: .
    ports:
      - 8080:8080
    environment:
      - MOCKBIN_REDIS=redis://redis:6379
      - MOCKBIN_PORT=8080
    links:
      - redis:redis
    depends_on:
      - redis
  redis:
    image: redis
    expose:
      - 6379
